zuul:
  routes:
    myProduct:
      path: /myProduct/**
      serviceId: PRODUCT
    user:
      path: /user/**
      serviceId: USER
  #全部服务忽略敏感头(全部服务可以传递cookie)    
  sensitive-headers:
server:
  port: 8083  
  
